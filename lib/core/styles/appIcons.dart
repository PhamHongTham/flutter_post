import 'package:flutter/material.dart';

import 'appColors.dart';

class AppIcons {
  static const prefixIconFirst = Icon(
    Icons.person,
    color: AppColor.colorIcons,
  );
  static const prefixIconSecond = Icon(
    Icons.email_rounded,
    color: AppColor.colorIcons,
  );
  static const prefixIconThird = Icon(
    Icons.phone_rounded,
    color: AppColor.colorIcons,
  );
}
